<!DOCTYPE html>
<html>
  <head>
    <title>ASHIMOTO App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <div class="nav">
        <%= link_to "Feesly", root_path, class: "nav__logo" %>
        <%= form_with url: search_posts_path, method: :get, local: true do |f| %>
          <%= f.text_field :keyword, placeholder:"キーワードで探す", class: "search" %>
          <% f.submit :keyword %>
        <% end %>
        <div class="nav__btn">
          <% if user_signed_in? %>
            <div class="nav__icon">
              <% if current_user.avatar? %>
                <%= image_tag current_user.avatar.url, size: '50x50', class:"icon" %>
              <% else %>
                <%= image_tag 'icon_dummy.jpg', size: '50x50', class:"icon" %>
              <% end %>
            </div>
            <% if current_page?(new_post_path) %>
              <%= link_to "投稿やめる", root_path, class:"new_post" %>
            <% else %>
              <%= link_to "投稿", new_post_path, class:"new_post" %>
            <% end %>
            <div class="hamburger-menu">
              <%= image_tag 'menu_open.png' %>
            </div>
            <div class="menu-list">
              <div class="menu-item">
                <ul>
                  <li><%= current_user.name %></li>
                  <li><%= link_to "トップページ", root_path, class:"link" %></li>
                  <li><%= link_to "自分の投稿", posts_path, class:"link" %></li>
                  <li><%= link_to "みんなの投稿", index_all_posts_path, class:"link" %></li>
                  <li><%= link_to "アカウント設定", edit_user_registration_path, class:"link" %></li>
                  <li><%= link_to "ログアウト", destroy_user_session_path, method: :delete, class:"link" %></li>
                </ul>
                <%= image_tag 'menu_close.png', class:"menu_close", size: '33x33' %>
              </div>
            </div
            <div class="overlay"></div>
          <% else %>
            <%= link_to "ゲストログイン", users_guest_sign_in_path, method: :post, class: 'post' %>
            <%= link_to "ログイン", new_user_session_path, class: 'post' %>
            <%= link_to "新規登録", new_user_registration_path, class: 'post' %>
          <% end %>
        </div>
      </div>
    </header>
    <% if flash.notice %>
      <p class="notice"><%= notice %></p>
    <% end %>
    <%= yield %>
    <footer>
        <p>ASHIMOTO SHARE</p>
        <p>© 2022 ASHIMOTO, Inc.</p>
    </footer>
  </body>
</html>
