<!DOCTYPE html>
<html>
  <head>
    <title>ASHIMOTO App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://code.jquery.com/jquery-3.6.1.js" integrity="sha256-3zlB5s2uwoUzrXK3BT7AX3FyvojsraNFxCc2vC/7pNI=" crossorigin="anonymous"></script>
  </head>

  <body>
    <header>
      <div class="nav">
        <%= link_to "ASHIMOTO SHARE", root_path, class: "nav__logo" %>
        <div class="nav__btn">
          <% if user_signed_in? %>
            <div class="nav__item">
              <p class="signed"><%= current_user.name %></p>
            </div>
            <div class="nav__item2">
              <% if current_user.avatar? %>
                <%= image_tag current_user.avatar.to_s, size: '50x50' %>
              <% else %>
                <%= image_tag 'icon_dummy.jpg', size: '50x50' %>
              <% end %>
            </div>
            <div class="hamburger-menu">
              <span></span>
              <span></span>
              <span></span>
            </div>
            <nav>
              
            </nav>
            <div class="overlay"></div>
          <% else %>
            <%= link_to "ログイン", new_user_session_path, class: 'post' %>
            <%= link_to "新規登録", new_user_registration_path, class: 'post' %>
          <% end %>
        </div>
      </div>
    </header>
    <% if flash.notice %>
      <p class="notice"><%= notice %></p>
    <% end %>
    <%= yield %>
    <footer>
        <p>ASHIMOTO SHARE</p>
        <p>© 2022 ASHIMOTO, Inc.</p>
    </footer>
  </body>
</html>
